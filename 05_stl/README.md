# 05_stl

本阶段学习 C++ 标准库中的常用容器、字符串、迭代器和算法。

当前知识点：

- E1: `std::vector`
- E2: `std::string` 常用操作
- E3: `std::map` / `std::unordered_map`
- E4: `std::set` / `std::unordered_set`
- E5: 迭代器
- E6: 常用算法 `std::sort`、`std::find`、`std::count`

本阶段重点不是背 API，而是理解标准库如何把常见工程问题封装起来，例如动态内存管理、元素生命周期、容量增长和遍历方式。
